server {
    listen 80;
    server_name _;
    root /app;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(json|png|jpg|jpeg|gif|ico|css|js|svg|woff|woff2|ttf)$ {
        expires 1d;
        add_header Cache-Control "public";
    }
}
